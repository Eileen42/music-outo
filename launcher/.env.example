# 런처 설정 (launcher/.env 로 복사해서 쓰세요)

# 사용자가 접속할 웹 주소. 배포 후 실제 도메인으로 교체.
# 개발 중에는 로컬 프론트엔드를 사용.
APP_URL=http://localhost:3000

# 로컬 백엔드 주소 (health check 용도). 일반적으로 변경 불필요.
BACKEND_URL=http://127.0.0.1:8000

# 백엔드 포트
BACKEND_PORT=8000
